# Rust Multithreaded Sample code
this is a demo rust multi threaded code

`imran82ali"`

we use following version of sysinfo crate
```
[dependencies]
sysinfo = "0.29"
```


now use `cargo run` for results